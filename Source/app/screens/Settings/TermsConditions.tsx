import * as React from "react";
import { ScrollView, Text } from "react-native";

export default function TermsConditions() {
  return (
    <ScrollView style={{ padding: 20, flex: 1 }}>
      <Text style={{ marginBottom: 20 }}>Terms</Text>
    </ScrollView>
  );
}
